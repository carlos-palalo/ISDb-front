import { BrokenVideoDirective } from './broken-video.directive';

describe('BrokenVideoDirective', () => {
  it('should create an instance', () => {
    const directive = new BrokenVideoDirective();
    expect(directive).toBeTruthy();
  });
});
